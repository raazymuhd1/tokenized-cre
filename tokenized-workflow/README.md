### CRE Config


### On-chain Read/Write
 see `actions/read.ts` and `actions/write.ts` files 

### secret managements
 see `secrets.yaml` or create new one on the root directory and defined all the secrets

### Helper
 see `helper` folder for reusable setup


### Contract abi
 see `contracts` folder to defined all the contracts abi

### execute the workflow
 see `main.ts` to execute the workflow